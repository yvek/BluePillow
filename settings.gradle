pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
plugins {
    id("com.android.settings") version "8.3.0"
}
android {
    minSdk = 23
    compileSdk = 34
}

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}
rootProject.name = "BluePillow"
include ':app'
include ':core-data'
include ':core-network'
include ':core-database'
